import { Box } from "@mui/system";

export default function NavBar(){
    return(
        <Box sx={{
            display: 'flex',
            marginBottom: '15px'
        }}>
            <header>ARC</header>
        </Box>
    )
}